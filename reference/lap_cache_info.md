# Report on the lapidary cache

Prints and returns the cache location and the size of each source
directory.

## Usage

``` r
lap_cache_info()
```

## Value

A data frame (invisibly) with columns `path`, `bytes`, `exists`.
