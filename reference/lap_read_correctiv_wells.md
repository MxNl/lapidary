# Read CORRECTIV well metadata as a `gwl_wells` layer

Read CORRECTIV well metadata as a `gwl_wells` layer

## Usage

``` r
lap_read_correctiv_wells()
```

## Value

A `gwl_wells` `sf` layer in EPSG:25832.
