# Validate a `gwl_ts` table

Validate a `gwl_ts` table

## Usage

``` r
validate_gwl_ts(x)
```

## Arguments

- x:

  Object to validate.

## Value

`x`, invisibly, if valid; otherwise an error is thrown.
