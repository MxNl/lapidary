# A named vector of the semantic accent colours

Handy for `scale_*_manual()` when mapping e.g. recharge vs discharge.

## Usage

``` r
lap_accents(variant = c("light", "dark"))
```

## Arguments

- variant:

  `"light"` or `"dark"`.

## Value

A named character vector.
