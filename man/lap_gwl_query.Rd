% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb.R
\name{lap_gwl_query}
\alias{lap_gwl_query}
\title{Run a lazy query against a source and collect the result}
\usage{
lap_gwl_query(
  fn,
  source = "gems-ger",
  version = "latest",
  which = c("gwl", "meteo"),
  con = NULL
)
}
\arguments{
\item{fn}{A function (or \verb{\\(tbl) ...} / \code{~ ...} formula) taking the lazy
table and returning a lazy or eager result.}

\item{source}{Source key. Default \code{"gems-ger"}.}

\item{version}{Version string, or \code{"latest"} (the newest built version).}

\item{which}{One of \code{"gwl"} (core levels) or \code{"meteo"} (forcing variables).}

\item{con}{Optional existing DuckDB connection. If \code{NULL} a fresh in-memory
connection is opened; close it afterwards with \code{\link[=lap_disconnect]{lap_disconnect()}} (which
also accepts the result of \code{dplyr} verbs applied to this table). For a
long-running app, open one connection yourself and pass it here. See also
\code{\link[=lap_gwl_query]{lap_gwl_query()}} for one-shot lazy pipelines.}
}
\value{
A tibble (the collected result).
}
\description{
Opens a DuckDB connection (or uses \code{con}), hands the lazy \code{\link[=lap_gwl_tbl]{lap_gwl_tbl()}} to
\code{fn}, collects the result, and closes the connection if it opened one. This
is the safe way to do a lazy filter/aggregate without managing the
connection lifetime yourself.
}
\examples{
\dontrun{
lap_gwl_query("gems-ger", fn = \(t) dplyr::filter(t, well_id == "MW_1"))
}
}
