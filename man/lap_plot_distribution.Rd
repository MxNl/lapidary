% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-distribution.R
\name{lap_plot_distribution}
\alias{lap_plot_distribution}
\title{Distribution of one indicator across wells}
\usage{
lap_plot_distribution(
  data,
  value,
  ...,
  group = NULL,
  geom = c("histogram", "density", "raincloud", "dots"),
  bins = 30,
  role = "magnitude",
  direction = 1,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  rug = FALSE,
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{A plain data frame with the value column (e.g. from
\code{\link[=lap_indicators]{lap_indicators()}} or \code{\link[=lap_summarise_wells]{lap_summarise_wells()}}).}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the column to plot.}

\item{...}{Passed to the underlying \verb{scale_*_lapidary_c()}.}

\item{group}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> optional split -
facets for \code{"histogram"} / \code{"density"}, rows for \code{"raincloud"} / \code{"dots"}.}

\item{geom}{\code{"histogram"} (default), \code{"density"}, \code{"raincloud"} or \code{"dots"}.
The last two need \pkg{ggdist}.}

\item{bins}{Histogram bin count. Default 30.}

\item{role, direction, robust, range}{Passed to the fill scale.}

\item{rug}{Add a marginal rug of the raw values.}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
A histogram, density, raincloud or dot plot of a single \verb{ind_*} column
(from \code{\link[=lap_indicators]{lap_indicators()}}), coloured on the same palette the maps use, so a
distribution panel reads consistently with a choropleth of the same metric.
}
\examples{
\dontrun{
ind <- lap_indicators(gems_ger_sample, "amplitude")
lap_plot_distribution(ind, ind_amplitude)
lap_plot_distribution(ind, ind_amplitude, geom = "raincloud")
}
}
\seealso{
\link{lap_plot_map}, \code{\link[=lap_indicator_registry]{lap_indicator_registry()}}
}
