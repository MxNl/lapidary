% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-period-ridges.R
\name{lap_plot_period_ridges}
\alias{lap_plot_period_ridges}
\title{Stacked period distributions (ridgelines)}
\usage{
lap_plot_period_ridges(
  data,
  value,
  ...,
  overlap = 1.4,
  interpret = TRUE,
  low_label = NULL,
  high_label = NULL,
  role = "magnitude",
  direction = 1,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{The long tibble from \code{\link[=lap_indicator_change]{lap_indicator_change()}}.}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the \verb{ind_*} column
to plot.}

\item{...}{Passed to the underlying \code{scale_fill_lapidary_c()}.}

\item{overlap}{Ridge overlap, passed to \code{ggridges} as \code{scale}: \code{1} makes the
tallest ridge just reach the next baseline; the default \code{1.4} overlaps the
ridges (the background-colour outline keeps them readable), \verb{> 2} is a
dense "Joy Division" stack.}

\item{interpret}{For the common indicators, label the value axis with what a
low vs a high value means (\code{"<low>  <-  Indicator  ->  <high>"}). \code{TRUE} by
default; has no effect for a column without a stored interpretation.
\code{low_label} / \code{high_label} set or override the wording for any column.}

\item{low_label, high_label}{Words for the low / high end of the value axis.
Both must resolve (from here or the registry) for the directional label to
appear.}

\item{role, direction, robust, range}{Passed to the fill scale.}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
Takes the long output of \code{\link[=lap_indicator_change]{lap_indicator_change()}} (\code{by} column(s), an
ordered \code{period} factor, and \verb{ind_*} columns) and draws one density ridge
per period, stacked earliest-on-top so reading down the plot follows time.
Each ridge is filled with the continuous lapidary palette along the value
axis (as the maps and \code{\link[=lap_plot_distribution]{lap_plot_distribution()}} are), outlined in the
background colour so overlapping ridges stay legible.
}
\details{
Needs \pkg{ggridges}.
}
\examples{
\dontrun{
chg <- lap_indicator_change(
  gems_ger_sample, "amplitude",
  periods = lap_period_windows(gems_ger_sample, "decade_per_decade")
)
lap_plot_period_ridges(chg, ind_amplitude)
lap_plot_period_ridges(chg, ind_trend_slope,
  low_label = "falling levels", high_label = "rising levels"
)
}
}
\seealso{
\code{\link[=lap_indicator_change]{lap_indicator_change()}}, \code{\link[=lap_plot_delta_map]{lap_plot_delta_map()}},
\code{\link[=lap_plot_distribution]{lap_plot_distribution()}}
}
