% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokens.R
\name{lap_tokens}
\alias{lap_tokens}
\title{lapidary design tokens}
\usage{
lap_tokens(variant = c("light", "dark"))
}
\arguments{
\item{variant}{\code{"light"} (default) or \code{"dark"}.}
}
\value{
A list with elements \code{variant}, \code{colour} (named colours), \code{font}
(family names + a logical \code{available}), \code{size} (type sizes, relative to a
base size of 1), \code{spacing} and \code{effect} (e.g. shadow sigma).
}
\description{
Returns a nested list of design tokens for one visual \code{variant}. Downstream
code should read tokens through this function rather than hard-coding
values, so that a restyle happens in one place.
}
\examples{
lap_tokens()$colour$recharge
lap_tokens("dark")$colour$panel
}
