% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{lap_summarise_wells}
\alias{lap_summarise_wells}
\title{Per-well (and per-period) groundwater summaries}
\usage{
lap_summarise_wells(
  x,
  by = c(well_id, year),
  value = gwl,
  expected_per_year = 52,
  collect = TRUE,
  indicators = NULL
)
}
\arguments{
\item{x}{A \code{gwl_ts}, data frame, or lazy \code{dplyr} tbl.}

\item{by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> grouping columns:
bare names, strings, or helpers. Defaults to \code{c(well_id, year)}; a \code{year}
column is derived from \code{date} if it is requested but absent.}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the single column to
summarise. Defaults to \code{gwl}.}

\item{expected_per_year}{Nominal observations per full year, for \code{coverage}.}

\item{collect}{If \code{x} is lazy, whether to \code{\link[dplyr:compute]{dplyr::collect()}} the result.}

\item{indicators}{Optional \verb{lap_ind_*()} function or list of them
(\code{\link[=lap_indicators]{lap_indicators()}}) to compute from the same time series and join onto the
result. Only valid for a well-level grouping (no \code{year} in \code{by}); forces an
in-memory computation if \code{x} is lazy.}
}
\value{
A tibble (or lazy tbl) with the grouping columns plus \code{min_gwl},
\code{max_gwl}, \code{mean_gwl}, \code{median_gwl}, \code{sd_gwl}, \code{n_obs}, \code{coverage}, and any
\verb{ind_*} columns from \code{indicators}.
}
\description{
Collapses a groundwater time series to one row per group, where the grouping
is usually the well and optionally the (water) year. Works both on an
in-memory \code{gwl_ts} / data frame and on a lazy \code{dplyr} table backed by DuckDB
(see \code{\link[=lap_gwl_tbl]{lap_gwl_tbl()}}); in the lazy case the aggregation is pushed down to the
database and you get a lazy result back unless \code{collect = TRUE}.
}
\details{
\code{coverage} is the fraction of the expected number of observations that are
actually present, using \code{expected_per_year} as the nominal count for a full
year (52 for weekly GEMS-GER data, 12 for monthly series). For groupings
without a year component \code{coverage} is \code{NA}.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
lap_summarise_wells(gems_ger_sample, by = c(well_id, year))
lap_summarise_wells(gems_ger_sample, by = "well_id", value = gwl)
lap_summarise_wells(
  gems_ger_sample,
  by = well_id, indicators = c(lap_ind_amplitude, lap_ind_trend)
)
}
