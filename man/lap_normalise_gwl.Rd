% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise.R
\name{lap_normalise_gwl}
\alias{lap_normalise_gwl}
\title{Normalise groundwater levels for cross-region comparison}
\usage{
lap_normalise_gwl(
  x,
  method = c("range", "zscore", "sgi"),
  value = gwl,
  group = well_id,
  date = "date",
  into = NULL
)
}
\arguments{
\item{x}{A \code{gwl_ts} (or data frame with \code{well_id}, \code{date}, \code{gwl}).}

\item{method}{One of \code{"range"}, \code{"zscore"}, \code{"sgi"}.}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the single column to
normalise. Default \code{gwl}.}

\item{group}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> columns identifying
an independent series. Default \code{well_id}.}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the date column, used
only by \code{"sgi"} (to deseasonalise). Default \code{date}.}

\item{into}{Name of the column to add. Default \code{"<value>_norm"}.}
}
\value{
\code{x} with the \code{into} column added.
}
\description{
Absolute groundwater levels (m above sea level, or depth below ground) are
not comparable between wells. \code{lap_normalise_gwl()} adds a \code{gwl_norm} column
holding a per-well normalised series, so that dynamics rather than absolute
magnitude can be compared and plotted.
}
\details{
Methods:
\describe{
\item{\code{"range"}}{Rescale each well to \verb{[0, 1]} using its own min/max.}
\item{\code{"zscore"}}{Subtract each well's mean and divide by its SD.}
\item{\code{"sgi"}}{Standardised Groundwater Index (Bloomfield & Marchant,
2013): a non-parametric normal-scores transform applied within each
calendar month of each well, yielding an approximately standard-normal,
deseasonalised index.}
}
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
head(lap_normalise_gwl(gems_ger_sample, "sgi"))
}
\references{
Bloomfield, J. P. and Marchant, B. P. (2013). Analysis of
groundwater drought building on the standardised precipitation index
approach. Hydrology and Earth System Sciences, 17, 4769-4787.
}
