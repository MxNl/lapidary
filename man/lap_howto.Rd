% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/i18n.R
\name{lap_howto}
\alias{lap_howto}
\title{A "how to read this chart" explainer string}
\usage{
lap_howto(builder, lang = NULL, variant = NULL, ...)
}
\arguments{
\item{builder}{Builder name without the \code{lap_plot_} prefix, e.g. \code{"hex_map"}.}

\item{lang}{Language code; defaults to \code{\link[=lap_lang]{lap_lang()}}.}

\item{variant}{\code{"light"} / \code{"dark"}; defaults to \code{\link[=lap_variant]{lap_variant()}}.}

\item{...}{Further named \code{{placeholder}} values passed to \code{\link[=lap_tr]{lap_tr()}}.}
}
\value{
A single string (may contain \verb{<span>} markup for \pkg{ggtext}).
}
\description{
Looks up \verb{howto_<builder>} in the string registry (see \code{\link[=lap_tr]{lap_tr()}}) and fills
the \code{{recharge}} / \code{{discharge}} / \code{{below}} / \code{{above}} colour placeholders
from the design tokens of \code{variant}, so the prose colour-matches the plot.
The plot builders call this for their default \code{plot.caption}; call it
directly to place the explainer elsewhere.
}
\examples{
lap_howto("hex_map")
lap_howto("hex_map", lang = "de")
}
