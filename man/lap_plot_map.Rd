% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hex-map.R
\name{lap_plot_map}
\alias{lap_plot_map}
\alias{lap_plot_hex_map}
\alias{lap_plot_point_map}
\title{lapidary map builders}
\usage{
lap_plot_hex_map(
  data,
  value,
  ...,
  role = NULL,
  direction = 1,
  midpoint = NULL,
  binned = TRUE,
  bins = 8,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  hull = TRUE,
  hull_shadow = TRUE,
  na_guide = TRUE,
  border_colour = NULL,
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)

lap_plot_point_map(
  data,
  value,
  ...,
  role = "magnitude",
  direction = 1,
  size = 1.6,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  basemap = TRUE,
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{For \code{lap_plot_hex_map()} an \code{sf} polygon layer (a hex grid); for
\code{lap_plot_point_map()} an \code{sf} POINT layer with the value column.}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the column to
colour by.}

\item{...}{Passed to the underlying \verb{scale_*_lapidary_c()} (e.g. \code{limits},
\code{breaks}).}

\item{role}{Palette role (see \code{\link[=lap_pal_roles]{lap_pal_roles()}}). Default: \code{"months"} for a
circular-month column, \code{"anomaly"} when \code{midpoint} is set, else
\code{"magnitude"}.}

\item{direction, midpoint, binned, bins, robust, range}{Passed to the scale.}

\item{hull, hull_shadow}{(\code{hex_map}) Draw the dissolved grid outline behind
the hexes, with a drop shadow (needs \pkg{ggfx}; degrades to no shadow).}

\item{na_guide}{(\code{hex_map}) Add a \code{\link[=lap_na_guide]{lap_na_guide()}} "no data" key when the
value column has \code{NA}s.}

\item{border_colour}{(\code{hex_map}) Hex border colour; default the background.}

\item{variant}{\code{"light"} / \code{"dark"}; defaults to \code{\link[=lap_variant]{lap_variant()}}.}

\item{lang}{Language code; defaults to \code{\link[=lap_lang]{lap_lang()}}.}

\item{annotate}{How-to-read explainer: \code{"caption"} (default), \code{"callout"},
\code{NA} to suppress, or a literal string. Default
\code{getOption("lapidary.annotate", "caption")}.}

\item{base_size, preset}{Base font size, or a \code{\link[=lap_preset_names]{lap_preset_names()}} value that
supplies it (e.g. \code{preset = "a1"} for poster-size text).}

\item{title, subtitle, caption}{Plot labels; \code{NULL} leaves them unset.}

\item{size}{(\code{point_map}) Point size.}

\item{basemap}{(\code{point_map}) Draw \code{\link[=lap_germany_border]{lap_germany_border()}} behind the points
(needs \pkg{rnaturalearth}).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
Two "status quo" builders that show one value per well over the whole
record on a map of Germany:
}
\details{
\itemize{
\item \code{lap_plot_hex_map()} - a choropleth of a hex grid from
\code{\link[=lap_aggregate_to_hex]{lap_aggregate_to_hex()}} (or \code{germany_hex_sample}). If a POINT layer is
passed it dispatches to \code{lap_plot_point_map()}.
\item \code{lap_plot_point_map()} - one coloured mark per monitoring well.
}

Both return a bare \link[ggplot2:ggplot]{ggplot2::ggplot}; both end with \code{\link[=theme_lapidary]{theme_lapidary()}}, a
single \verb{scale_*_lapidary_c()} and a how-to-read \code{plot.caption} (see
\code{annotate}).
}
\examples{
\dontrun{
data(germany_hex_sample, package = "lapidary")
lap_plot_hex_map(germany_hex_sample, mean_gwl)
lap_plot_hex_map(germany_hex_sample, n_wells, role = "density")
}
}
\seealso{
\code{\link[=lap_aggregate_to_hex]{lap_aggregate_to_hex()}}, \code{\link[=lap_annotate_howto]{lap_annotate_howto()}}
}
