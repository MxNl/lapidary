% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb.R
\name{lap_csvs_to_parquet}
\alias{lap_csvs_to_parquet}
\title{Convert a directory of per-well CSV files into one Parquet file}
\usage{
lap_csvs_to_parquet(
  csv_glob,
  out_path,
  select_sql = "*",
  filename = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{csv_glob}{Glob pattern matching the CSV files.}

\item{out_path}{Output Parquet file.}

\item{select_sql}{SQL select-list mapping source columns to the canonical
schema, e.g. \code{"filename AS src, GWL AS gwl"}.}

\item{filename}{Whether to expose the source path as a \code{filename} column
(needed when the well id is encoded in the file name).}

\item{overwrite}{Overwrite an existing output.}
}
\value{
\code{out_path}, invisibly.
}
\description{
DuckDB globs and unions the CSVs in a single query. Column selection and
renaming are handled by \code{select_sql} (a SQL \code{SELECT} list).
}
