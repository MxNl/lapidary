% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{lap_gw_trend}
\alias{lap_gw_trend}
\title{Non-parametric trend of groundwater time series}
\usage{
lap_gw_trend(
  x,
  value = mean_gwl,
  time = year,
  by = well_id,
  min_n = 10L,
  conf_level = 0.95
)
}
\arguments{
\item{x}{A data frame with a time column, a value column and grouping
column(s).}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the single value
column. Default \code{mean_gwl}.}

\item{time}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the single time
column (numeric years, or Date). Default \code{year}.}

\item{by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> grouping column(s).
Default \code{well_id}.}

\item{min_n}{Minimum number of finite observations required per group.}

\item{conf_level}{Confidence level for the slope confidence interval.}
}
\value{
A tibble with one row per group: \code{n}, \code{slope} (value units per year),
\code{slope_lower}, \code{slope_upper}, \code{intercept}, \code{tau}, \code{p_value},
\code{significant} (logical at \code{1 - conf_level}), \code{direction}.
}
\description{
Computes the Theil-Sen slope estimator together with the Mann-Kendall trend
test (with tie and, optionally, autocorrelation correction) for each series.
This is the method used in the KLIBIW low-water assessments and is robust to
outliers and non-normal residuals.
}
\details{
Input is typically an annual series - e.g. the \code{mean_gwl} column of
\code{\link[=lap_summarise_wells]{lap_summarise_wells()}} grouped by \code{c(well_id, year)} - but any regular-ish
series works.
}
\examples{
set.seed(1)
df <- data.frame(
  well_id = rep(c("a", "b"), each = 30),
  year = rep(1991:2020, 2),
  mean_gwl = c(
    50 - 0.1 * (0:29) + rnorm(30, 0, 0.2),
    20 + rnorm(30, 0, 0.2)
  )
)
lap_gw_trend(df)
}
