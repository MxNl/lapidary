% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-delta-map.R
\name{lap_plot_delta_map}
\alias{lap_plot_delta_map}
\title{Map the change in an indicator between two periods}
\usage{
lap_plot_delta_map(
  data,
  value,
  ...,
  display = c("change", "paired", "arrow"),
  direction = 1,
  binned = TRUE,
  bins = 8,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  hull = TRUE,
  hull_shadow = TRUE,
  na_guide = TRUE,
  border_colour = NULL,
  margin = c("none", "histogram", "density", "raincloud"),
  margin_side = c("bottom", "right"),
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{An \code{sf} polygon layer (a hex grid) with the delta columns.}

\item{value}{The base indicator column, as a bare name or string (e.g.
\code{ind_amplitude}); the \verb{_<from>} / \verb{_<to>} / \verb{_change} columns are resolved
from it.}

\item{...}{Passed to the underlying \code{scale_fill_lapidary_c()}.}

\item{display}{\code{"change"} (default) a divergent choropleth of \verb{_change}
about zero; \code{"paired"} two shared-scale period maps (returns a
\link[patchwork:patchwork-package]{patchwork::patchwork}); \code{"arrow"} a per-hexagon glyph whose length and
direction encode the change.}

\item{direction, binned, bins, robust, range}{Passed to the fill scale.}

\item{hull, hull_shadow, na_guide, border_colour}{As in \code{\link[=lap_plot_hex_map]{lap_plot_hex_map()}}.}

\item{margin, margin_side}{Attach a marginal distribution of the mapped
\verb{_change} values (only for \code{display = "change"}); see \code{\link[=lap_attach_margin]{lap_attach_margin()}}.}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}, or a \link[patchwork:patchwork-package]{patchwork::patchwork} for
\code{display = "paired"} or when \code{margin} is set.
}
\description{
Visualises the output of \code{\link[=lap_indicator_delta]{lap_indicator_delta()}} aggregated onto a hex grid:
the per-hexagon change in one indicator between a \code{from} and a \code{to} period.
}
\details{
\code{data} is an \code{sf} hex layer carrying the wide \verb{<value>_<from>},
\verb{<value>_<to>} and \verb{<value>_change} columns - build it by joining
\code{\link[=lap_indicator_delta]{lap_indicator_delta()}} to the wells and passing it through
\code{\link[=lap_aggregate_to_hex]{lap_aggregate_to_hex()}}. \code{value} is the \strong{base} indicator column name
(without the period / \verb{_change} suffix), e.g. \code{ind_amplitude}.
}
\examples{
\dontrun{
chg <- lap_indicator_change(
  gems_ger_sample, "amplitude",
  periods = list(reference = c(1991, 2010), recent = c(2011, 2022))
)
dl <- lap_indicator_delta(chg, "reference", "recent")
hex <- lap_aggregate_to_hex(gems_ger_wells_sample, dl)
lap_plot_delta_map(hex, ind_amplitude)
lap_plot_delta_map(hex, ind_amplitude, display = "paired")
}
}
\seealso{
\code{\link[=lap_indicator_delta]{lap_indicator_delta()}}, \code{\link[=lap_plot_change_scatter]{lap_plot_change_scatter()}},
\code{\link[=lap_plot_period_ridges]{lap_plot_period_ridges()}}
}
