% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsave.R
\name{ggsave_lapidary}
\alias{ggsave_lapidary}
\title{Save a lapidary plot at a named size preset}
\usage{
ggsave_lapidary(plot, filename, preset = "web", ...)
}
\arguments{
\item{plot}{A ggplot (or patchwork) object.}

\item{filename}{Output path. The device is inferred from the extension
(\code{.png}, \code{.pdf}, ...).}

\item{preset}{A name from \code{\link[=lap_preset_names]{lap_preset_names()}}.}

\item{...}{Overrides passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (\code{width}, \code{height},
\code{dpi}, ...).}
}
\value{
\code{filename}, invisibly.
}
\description{
Wraps \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with the poster/web size + dpi presets and,
crucially, sets \code{showtext}'s DPI to match so text metrics are correct in
print output (the recurring gotcha in the prototype). Uses the \pkg{ragg}
PNG device when available for crisper text.
}
\details{
This does \strong{not} re-theme the plot; build the plot with
\code{theme_lapidary(base_size = lap_preset(preset)$base_size)} (the
milestone-2 builders take a \code{preset}/\code{base_size} argument that does this).
}
