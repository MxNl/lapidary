% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference-period.R
\name{lap_add_reference_period}
\alias{lap_add_reference_period}
\title{Tag rows with a reference-period label}
\usage{
lap_add_reference_period(
  x,
  periods = lap_reference_periods()["Z1"],
  date_col = date,
  year_col = NULL,
  drop = FALSE
)
}
\arguments{
\item{x}{A \code{gwl_ts} or data frame.}

\item{periods}{Named list of \code{c(start_year, end_year)} pairs. Defaults to
\code{lap_reference_periods()["Z1"]}.}

\item{date_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> date column to
derive the year from (bare name or string). Ignored if \code{year_col} is given.
Defaults to \code{date}.}

\item{year_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> optional integer
year column to use directly instead of \code{date_col}.}

\item{drop}{If \code{TRUE}, keep only rows that fall inside a period.}
}
\value{
\code{x} with a \code{reference_period} factor column (levels = \code{names(periods)}).
}
\description{
Adds a \code{reference_period} column marking which configured period each
observation falls into (by calendar year of \code{date_col}, or by \code{year_col}
if supplied). Rows outside every period get \code{NA}. Periods must not overlap.
}
\examples{
df <- data.frame(date = as.Date(paste0(1985:1995, "-06-15")))
lap_add_reference_period(df)
lap_add_reference_period(df, periods = lap_reference_periods()[c("Z0", "Z1")], drop = TRUE)
}
