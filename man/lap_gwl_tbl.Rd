% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb.R
\name{lap_gwl_tbl}
\alias{lap_gwl_tbl}
\title{Lazy table over a source's groundwater Parquet dataset}
\usage{
lap_gwl_tbl(
  source = "gems-ger",
  version = "latest",
  which = c("gwl", "meteo"),
  con = NULL
)
}
\arguments{
\item{source}{Source key. Default \code{"gems-ger"}.}

\item{version}{Version string or \code{"latest"}.}

\item{which}{One of \code{"gwl"} (core levels) or \code{"meteo"} (forcing variables).}

\item{con}{Optional existing DuckDB connection. If \code{NULL} a fresh in-memory
connection is opened and attached to the result as \code{attr(x, "gwl_con")};
remember to \code{\link[=lap_disconnect]{lap_disconnect()}} it.}
}
\value{
A lazy \code{tbl} with an attached connection attribute.
}
\description{
Returns a lazy \code{dplyr} table (backed by DuckDB) over the core groundwater
Parquet file for \code{source}. Filtering and aggregation on the result are
pushed down to DuckDB; call \code{\link[dplyr:compute]{dplyr::collect()}} to materialise, or use
\code{\link[=lap_read_gwl]{lap_read_gwl()}} which returns a validated \code{gwl_ts}.
}
