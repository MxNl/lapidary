% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{lap_indicator_delta}
\alias{lap_indicator_delta}
\title{Difference indicators between two periods}
\usage{
lap_indicator_delta(change, from, to, by = well_id)
}
\arguments{
\item{change}{The long tibble from \code{\link[=lap_indicator_change]{lap_indicator_change()}}.}

\item{from, to}{Period labels present in \code{change$period}.}

\item{by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the well-identifying
column(s). Default \code{well_id}.}
}
\value{
A tibble, one row per \code{by}, with \verb{<col>_<from>}, \verb{<col>_<to>} and
(where meaningful) \verb{<col>_change} for each \verb{ind_*} column.
}
\description{
Takes the long output of \code{\link[=lap_indicator_change]{lap_indicator_change()}} and returns one row per
well with, for each indicator column, its value in \code{from}, its value in \code{to},
and the change - differenced according to the indicator's kind (a plain
difference; a signed month difference in \verb{(-6, 6]} for the circular month
columns; nothing for p-values and \code{ind_step_year}).
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
chg <- lap_indicator_change(
  gems_ger_sample, c("amplitude", "extreme_months"),
  periods = list(reference = c(1991, 2010), recent = c(2011, 2022))
)
lap_indicator_delta(chg, "reference", "recent")
}
