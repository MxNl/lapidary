% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{lap_summarise_calendar}
\alias{lap_summarise_calendar}
\title{Sum event columns into a year x month/week calendar grid}
\usage{
lap_summarise_calendar(x, ..., date = "date", period = c("month", "week"))
}
\arguments{
\item{x}{A data frame with a \code{date} column and the columns to sum.}

\item{...}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> one or more
numeric or logical columns to sum, e.g. \verb{is_new_min, is_new_max}.}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the date column.
Default \code{date}.}

\item{period}{\code{"month"} (default, \code{unit} is 1-12) or \code{"week"} (\code{unit} is
the ISO week number, paired with its ISO week-year so the Dec/Jan
week-53 edge case lands in the right year).}
}
\value{
A tibble: \code{year}, \code{unit}, \code{name} (the summed column, with a
leading \code{is_} stripped for a cleaner label) and \code{n} (its sum in that
bucket). A \verb{(year, unit, name)} row only appears when at least one row
contributed a non-\code{NA} value for that column in that bucket; a bucket
with rows but no flagged event still gets a real \code{n = 0}, distinct from
a bucket where every contributing row was \code{NA} (e.g. every well's first
year, see \code{\link[=lap_add_record_flags]{lap_add_record_flags()}}), which produces no row at all.
Columns from the same call can drop independently - if one column is
all-\code{NA} in a bucket but another isn't, only the all-\code{NA} column's row
is omitted.
}
\description{
Turns one or more row-level numeric or logical columns (e.g.
\code{\link[=lap_add_record_flags]{lap_add_record_flags()}}'s \code{is_new_min} / \code{is_new_max}) into a long
\code{year | unit | name | n} table - the sum of each column, across \emph{every}
row of \code{x} (not per well), in every calendar bucket. Feed the result to
\code{\link[=lap_plot_calendar]{lap_plot_calendar()}}.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
flagged <- lap_add_record_flags(gems_ger_sample)
# one column: the net balance of new-high vs new-low record years
lap_summarise_calendar(flagged, record_balance)
# or several at once: each series' own magnitude, kept separate
lap_summarise_calendar(flagged, is_new_min, is_new_max)
}
\seealso{
\code{\link[=lap_add_record_flags]{lap_add_record_flags()}}, \code{\link[=lap_plot_calendar]{lap_plot_calendar()}}
}
