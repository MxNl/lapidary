% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{lap_colourbar_guide}
\alias{lap_colourbar_guide}
\title{A long, thin smooth colour-bar legend guide}
\usage{
lap_colourbar_guide(
  length = 18,
  thickness = 0.55,
  title_gap = 0.9,
  label_gap = 1,
  tick_length = 0.2,
  order = 1,
  variant = NULL,
  ...
)
}
\arguments{
\item{length, thickness}{Bar length and thickness, in text \code{"lines"} (so they
scale with the legend text size). Defaults 18 and 0.55.}

\item{title_gap, label_gap, tick_length}{Space (in \code{"lines"}) below the legend
title, to the left of the break labels, and the tick-mark length. Defaults
0.9, 1.0 and 0.2.}

\item{order}{Guide order (default 1), so it sits above a \code{\link[=lap_na_guide]{lap_na_guide()}} key.}

\item{variant}{\code{"light"} / \code{"dark"} for the frame / tick colour. Defaults to
\code{\link[=lap_variant]{lap_variant()}}.}

\item{...}{Passed to \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colourbar()}}.}
}
\value{
A ggplot2 guide.
}
\description{
The \code{\link[=lap_coloursteps_guide]{lap_coloursteps_guide()}} styling (tall, narrow, framed, ticked) for a
smooth (non-binned) scale - \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colourbar()}} instead of
\code{\link[ggplot2:guide_coloursteps]{ggplot2::guide_coloursteps()}}. Use it when a scale is built with
\code{binned = FALSE}, or for a bespoke continuous scale such as
\code{\link[=lap_plot_calendar]{lap_plot_calendar()}}'s divergent mode.
}
