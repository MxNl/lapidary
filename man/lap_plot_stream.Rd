% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-stream.R
\name{lap_plot_stream}
\alias{lap_plot_stream}
\title{Proportional streamgraph of a composition over time}
\usage{
lap_plot_stream(
  data,
  category,
  x = date,
  ...,
  smooth = 0,
  curve = TRUE,
  n_grid = 400,
  border_colour = NA,
  role = "anomaly",
  direction = -1,
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", NA),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{The tibble from \code{\link[=lap_summarise_composition]{lap_summarise_composition()}} (\code{x}, \code{category},
\code{n}).}

\item{category}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the categorical
column to stack, ideally an ordered factor (its level order becomes the
bottom-to-top stacking order).}

\item{x}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the time-bucket column.
Default \code{date}.}

\item{...}{Passed to the underlying \code{scale_fill_lapidary_d()}. Its legend
defaults to \code{guide = guide_legend(reverse = TRUE)} so the legend reads
low-to-high bottom-to-top too, matching the stack; pass your own \code{guide}
to override.}

\item{smooth}{Centred rolling-mean window (in buckets) applied to each
category's \code{n} before stacking - tames real week-to-week/month-to-month
\emph{noise}. \code{0}/\code{1} (default) leaves the values as-is. Use
\code{\link[=lap_summarise_composition]{lap_summarise_composition()}}'s \verb{period =} first if what you want is a
coarser \emph{time bucket} (e.g. \code{"month"} or \code{"year"}), not smoothing of a
weekly series.}

\item{curve}{Round the stream's edges into a smooth curve by
spline-interpolating each category's series onto a finer x grid before
drawing, instead of the straight-line segments a plain stacked area chart
draws between buckets. \code{TRUE} by default; set \code{FALSE} for the literal,
unsmoothed polygon.}

\item{n_grid}{Number of x positions in that finer grid (at least the number
of buckets already present). Default 400.}

\item{border_colour}{Outline colour drawn between adjacent bands (a thin
cut-out seam so touching bands stay visually separated). \code{NA} (default)
draws no outline.}

\item{role, direction}{Passed to the fill scale. Default \code{role = "anomaly"}
(a divergent palette), a good fit for an odd number of ordered categories
centred on a "normal" middle band; \code{direction = -1} puts the low end of
the palette on the \emph{high} category (so, for \code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}},
"very low" reads as the warm/dry colour and "very high" as the cool/wet
one - the hydrological drought-index convention).}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
A 100\%-stacked, smooth area chart of the output of
\code{\link[=lap_summarise_composition]{lap_summarise_composition()}} - e.g. the share of wells in each
\code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}} percentile band, week by week. Every column
sums to 100\%, so the shape shows how the \emph{mix} shifts over time, not
absolute counts.
}
\examples{
\dontrun{
gems_ger_sample |>
  lap_add_quantile_class() |>
  lap_summarise_composition(gwl_class) |>
  lap_plot_stream(gwl_class)

# coarser time bucket + no extra curve smoothing
gems_ger_sample |>
  lap_add_quantile_class() |>
  lap_summarise_composition(gwl_class, period = "month") |>
  lap_plot_stream(gwl_class, curve = FALSE)
}
}
\seealso{
\code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}}, \code{\link[=lap_summarise_composition]{lap_summarise_composition()}}
}
