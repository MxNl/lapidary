% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference-period.R
\name{lap_period_windows}
\alias{lap_period_windows}
\title{Derive comparison windows from a record's date range}
\usage{
lap_period_windows(
  x,
  scheme = c("first_vs_last_decade", "first_vs_last_half", "decade_per_decade"),
  date = "date",
  width = 10L
)
}
\arguments{
\item{x}{A data frame with a date column, or a length-2 numeric vector
\code{c(first_year, last_year)}.}

\item{scheme}{One of:
\itemize{
\item \code{"first_vs_last_decade"} - the first and last \code{width} years;
\item \code{"first_vs_last_half"} - the record split at its midpoint;
\item \code{"decade_per_decade"} - one window per 10-year block from the first
year of the record, the last block clipped to the end. Names are the
actual spans, e.g. \code{"1991-2000"}.
}}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the date column used
to derive the year range when \code{x} is a data frame. Default \code{date}.}

\item{width}{Window length in years for \code{"first_vs_last_decade"}. Default 10.}
}
\value{
A named list of \code{c(start_year, end_year)} integer pairs in
chronological order (\code{first} before \code{last}), validated for
\code{\link[=lap_indicator_change]{lap_indicator_change()}} (overlaps allowed).
}
\description{
Builds a named \code{periods} list (the shape \code{\link[=lap_indicator_change]{lap_indicator_change()}} and
\code{\link[=lap_add_reference_period]{lap_add_reference_period()}} expect) from the span of a groundwater record,
so you do not have to write year pairs by hand. Unlike
\code{\link[=lap_reference_periods]{lap_reference_periods()}} (fixed WMO climate normals), these windows adapt to
the data.
}
\details{
\code{"decade_per_decade"} can return more than two windows; pass any two
of its labels to \code{\link[=lap_indicator_delta]{lap_indicator_delta()}}. The non-overlapping schemes
(\code{"first_vs_last_half"} and \code{"decade_per_decade"}) also work as the
\code{periods} argument of \code{\link[=lap_add_reference_period]{lap_add_reference_period()}}.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
lap_period_windows(gems_ger_sample, "first_vs_last_decade")
lap_period_windows(c(1991, 2022), "decade_per_decade")
lap_indicator_change(
  gems_ger_sample, c("amplitude", "trend"),
  periods = lap_period_windows(gems_ger_sample, "first_vs_last_half")
)
}
\seealso{
\code{\link[=lap_reference_periods]{lap_reference_periods()}}, \code{\link[=lap_indicator_change]{lap_indicator_change()}},
\code{\link[=lap_add_reference_period]{lap_add_reference_period()}}
}
