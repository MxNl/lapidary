% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-change-scatter.R
\name{lap_plot_change_scatter}
\alias{lap_plot_change_scatter}
\title{Starting value against its change}
\usage{
lap_plot_change_scatter(
  data,
  value,
  ...,
  smooth = TRUE,
  rug = FALSE,
  direction = 1,
  binned = TRUE,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{The wide tibble from \code{\link[=lap_indicator_delta]{lap_indicator_delta()}}.}

\item{value}{The base indicator column, as a bare name or string.}

\item{...}{Passed to the underlying \code{scale_colour_lapidary_c()}.}

\item{smooth}{Add a linear \code{geom_smooth()} fit. Default \code{TRUE}.}

\item{rug}{Add marginal rugs.}

\item{direction, binned, robust, range}{Passed to the colour scale.}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
A well-level scatter of an indicator's value in the \code{from} period (x)
against how much it changed by the \code{to} period (y), from the wide output of
\code{\link[=lap_indicator_delta]{lap_indicator_delta()}}. A tilted cloud means wells that started high
changed differently from wells that started low.
}
\details{
\code{data} carries the wide \verb{<value>_<from>} / \verb{<value>_change} columns; \code{value}
is the \strong{base} indicator column name (e.g. \code{ind_amplitude}).
}
\examples{
\dontrun{
chg <- lap_indicator_change(
  gems_ger_sample, "amplitude",
  periods = list(reference = c(1991, 2010), recent = c(2011, 2022))
)
dl <- lap_indicator_delta(chg, "reference", "recent")
lap_plot_change_scatter(dl, ind_amplitude)
}
}
\seealso{
\code{\link[=lap_indicator_delta]{lap_indicator_delta()}}, \code{\link[=lap_plot_delta_map]{lap_plot_delta_map()}}
}
