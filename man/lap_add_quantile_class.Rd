% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile-class.R
\name{lap_add_quantile_class}
\alias{lap_add_quantile_class}
\title{Classify groundwater levels into per-well percentile bands}
\usage{
lap_add_quantile_class(
  x,
  value = gwl,
  group = well_id,
  date = "date",
  breaks = c(0, 0.05, 0.1, 0.25, 0.75, 0.9, 0.95, 1),
  labels = NULL,
  lang = NULL,
  reference = NULL,
  into = NULL
)
}
\arguments{
\item{x}{A \code{gwl_ts} (or data frame with \code{well_id}, \code{date}, \code{gwl}).}

\item{value}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the column to
classify. Default \code{gwl}.}

\item{group}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> columns identifying
an independent series (breakpoints are computed once per group). Default
\code{well_id}.}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the date column,
used only when \code{reference} is given. Default \code{date}.}

\item{breaks}{Percentile cut points, \verb{[0, 1]}, increasing. Default
\code{c(0, .05, .10, .25, .75, .90, .95, 1)} (7 bands).}

\item{labels}{Band labels, one shorter than \code{breaks}. \code{NULL} (default) uses
the built-in bilingual \code{gwl_class_labels} (see \code{lang}); pass your own
7-vector to override.}

\item{lang}{Language for the default \code{labels}; defaults to \code{\link[=lap_lang]{lap_lang()}}.
Ignored if \code{labels} is given.}

\item{reference}{\code{NULL} (default) computes breakpoints from a well's whole
record. Or \code{c(start_year, end_year)} (or a single named period, e.g.
\code{lap_reference_periods()["Z1"]}): breakpoints are computed from only the
rows in that window, then \emph{every} row (in or out of it) is classified
against them - a fixed "normal" the way a climate normal works.}

\item{into}{Name of the column to add. Default \code{"<value>_class"}.}
}
\value{
\code{x} with the \code{into} column added: an ordered factor, levels from
low to high. A group whose reference values are all identical (nothing to
split into bands) gets \code{NA} for every row, with a warning.
}
\description{
Tags every row with which percentile band of \emph{that well's own} distribution
it falls into - the German hydrological-service groundwater-level
classification ("Grundwasserstandsklassen") used in weekly LfU / BGR / HLNUG
bulletins: very low / low / below normal / normal / above normal / high /
very high, split at the 5th, 10th, 25th, 75th, 90th and 95th percentile.
}
\details{
The breakpoints are computed independently for each well (a well's own
history is the yardstick for what counts as "normal" for it), then every
row of that well is classified against them - so the result is comparable
across wells with very different absolute levels.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
head(lap_add_quantile_class(gems_ger_sample))
}
\references{
Bloomfield, J. P. and Marchant, B. P. (2013) as for
\code{\link[=lap_normalise_gwl]{lap_normalise_gwl()}}; the band scheme follows the German state
groundwater services' weekly bulletin convention (e.g. Bayerisches
Landesamt fuer Umwelt, "Grundwasserstandsklassen").
}
\seealso{
\code{\link[=lap_summarise_composition]{lap_summarise_composition()}}, \code{\link[=lap_plot_stream]{lap_plot_stream()}}
}
