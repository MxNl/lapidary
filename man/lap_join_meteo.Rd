% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-gems-ger.R
\name{lap_join_meteo}
\alias{lap_join_meteo}
\title{Join GEMS-GER meteorological forcings onto a groundwater series}
\usage{
lap_join_meteo(x, vars = "all", version = "latest")
}
\arguments{
\item{x}{A \code{gwl_ts} from \code{\link[=lap_read_gems_ger]{lap_read_gems_ger()}} (its \code{source} must be
\code{"gems-ger"}).}

\item{vars}{Forcing columns to join. Either \code{"all"}, or a character vector of
names from \code{gems_ger_meteo_cols} (\code{HYRAS_pr}, \code{DWD_evapo_p}, ...),
optionally \strong{named} to rename on join, e.g.
\code{c(precip = "HYRAS_pr", pet = "DWD_evapo_p")}.}

\item{version}{Version key or \code{"latest"}.}
}
\value{
\code{x} with the forcing columns added (a \code{gwl_ts}).
}
\description{
Reads the requested forcing columns from \code{meteo.parquet} (build it with
\code{\link[=lap_gems_ger_build_parquet]{lap_gems_ger_build_parquet()}} \code{meteo = TRUE}) and left-joins them onto a
GEMS-GER \code{gwl_ts} by \code{well_id} and \code{date}. The result is still a \code{gwl_ts};
the joined columns are what \code{\link[=lap_ind_climate_signal]{lap_ind_climate_signal()}} needs as its \code{driver}.
}
\examples{
\dontrun{
lap_read_gems_ger() |>
  lap_join_meteo(c(precip = "HYRAS_pr")) |>
  lap_normalise_gwl("sgi") |>
  lap_indicators("climate_signal", value = gwl_norm, driver = precip)
}
}
\seealso{
\code{\link[=lap_ind_climate_signal]{lap_ind_climate_signal()}}, \code{\link[=lap_gems_ger_build_parquet]{lap_gems_ger_build_parquet()}}
}
