% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{lap_summarise_composition}
\alias{lap_summarise_composition}
\title{Share of wells in each category, per time bucket}
\usage{
lap_summarise_composition(
  x,
  category,
  date = "date",
  period = c("week", "month", "year"),
  by = well_id
)
}
\arguments{
\item{x}{A data frame with \code{by}, \code{date} and \code{category} columns (e.g. the
output of \code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}}).}

\item{category}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the categorical
column to tally (ideally a factor, so empty categories still get a \code{0}
row rather than being silently dropped from a bucket).}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the date column.
Default \code{date}.}

\item{period}{Bucket width: \code{"week"} (default, ISO weeks, Monday start),
\code{"month"} or \code{"year"}.}

\item{by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the well-identifying
column(s). Default \code{well_id}.}
}
\value{
A tibble: \code{date} (the bucket start), \code{category}, \code{n} (well count)
and \code{share} (\code{n} divided by the bucket total).
}
\description{
Turns a row-level categorical column (e.g. \code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}}'s
\code{gwl_class}, or any other per-well state - drought / rising / falling, ...)
into a composition-over-time table: for each \code{period} bucket, how many
wells sat in each \code{category}, and what share of that bucket they were. Feed
the result to \code{\link[=lap_plot_stream]{lap_plot_stream()}}.
}
\details{
If \code{x} has more than one row per well within a bucket (raw daily data, or a
\code{period} coarser than \code{x}'s own resolution), only the most recent row per
well per bucket counts - so a well contributes exactly once to each bucket
regardless of how finely \code{x} is sampled.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
gems_ger_sample |>
  lap_add_quantile_class() |>
  lap_summarise_composition(gwl_class)
}
\seealso{
\code{\link[=lap_add_quantile_class]{lap_add_quantile_class()}}, \code{\link[=lap_plot_stream]{lap_plot_stream()}}
}
