% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{lap_add_indicators}
\alias{lap_add_indicators}
\title{Append time-series indicators to a well-level table}
\usage{
lap_add_indicators(data, x, .funs, by = well_id, value = gwl, date = date)
}
\arguments{
\item{data}{A well-level table (e.g. from \code{\link[=lap_summarise_wells]{lap_summarise_wells()}} grouped by
\code{well_id}, or a \code{gwl_wells} layer).}

\item{x}{The time series the indicators are computed from.}

\item{.funs}{A \verb{lap_ind_*} function or a list of them.}

\item{by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> join key(s), present in
both \code{data} and \code{x}. Default \code{well_id}.}

\item{value, date}{Passed to \code{\link[=lap_indicators]{lap_indicators()}}.}
}
\value{
\code{data} with the indicator columns added.
}
\description{
For building an indicator set step by step: takes an existing well-level
table, computes the requested indicators from the time series, and left-joins
them on \code{by}.
}
\examples{
data(gems_ger_sample, package = "lapidary", envir = environment())
base <- lap_summarise_wells(gems_ger_sample, by = well_id)
base |>
  lap_add_indicators(gems_ger_sample, lap_ind_amplitude) |>
  lap_add_indicators(gems_ger_sample, lap_ind_trend)
}
