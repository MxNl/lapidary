% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{lap_annotate_howto}
\alias{lap_annotate_howto}
\title{Add a "how to read this chart" explainer to a plot}
\usage{
lap_annotate_howto(
  plot,
  key = NULL,
  text = NULL,
  placement = c("caption", "callout"),
  corner = c("bottom-right", "bottom-left", "top-right", "top-left"),
  lang = NULL,
  variant = NULL,
  tokens = NULL
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{key}{Builder name without the \code{lap_plot_} prefix (e.g. \code{"hex_map"}),
used to look up the registry string. Ignored if \code{text} is given.}

\item{text}{Explainer text (may contain \verb{<span>} markup for \pkg{ggtext}).
\code{NULL} (default) uses \code{lap_howto(key)}.}

\item{placement}{\code{"caption"} (append to \code{plot.caption}) or \code{"callout"} (an
on-panel box).}

\item{corner}{For \code{"callout"}: which corner. One of \code{"bottom-right"}
(default), \code{"bottom-left"}, \code{"top-right"}, \code{"top-left"}.}

\item{lang, variant}{Passed to \code{\link[=lap_howto]{lap_howto()}} when \code{text} is \code{NULL}.}

\item{tokens}{A \code{\link[=lap_tokens]{lap_tokens()}} list; computed from \code{variant} if \code{NULL}.}
}
\value{
\code{plot} with the annotation added.
}
\description{
Places a short explainer either in \code{plot.caption} (default) or as an
on-panel corner box. The \link{lap_plot_map} builders call this for their default
\code{annotate = "caption"}; call it directly to override placement or add an
explainer to a plot the builders did not make.
}
\examples{
\dontrun{
library(ggplot2)
data(germany_hex_sample, package = "lapidary")
lap_plot_hex_map(germany_hex_sample, mean_gwl, annotate = NA) |>
  lap_annotate_howto("hex_map", placement = "callout")
}
}
\seealso{
\code{\link[=lap_howto]{lap_howto()}}, \link{lap_plot_map}
}
