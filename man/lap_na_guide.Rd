% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{lap_na_guide}
\alias{lap_na_guide}
\title{Add a "no data" key for NA regions}
\usage{
lap_na_guide(
  label = lap_tr("no_data"),
  colour = NULL,
  variant = NULL,
  order = 2
)
}
\arguments{
\item{label}{Key label. Defaults to the localised \code{"no data"}.}

\item{colour}{Swatch colour. \code{NULL} (default) uses the \code{variant}'s token
\code{missing} colour (matches the scale's \code{na.value}).}

\item{variant}{\code{"light"} / \code{"dark"}. Defaults to \code{\link[=lap_variant]{lap_variant()}}.}

\item{order}{Legend order (passed to \code{\link[ggplot2:guide_legend]{ggplot2::guide_legend()}}); the default
2 keeps the NA key below a \code{\link[=lap_coloursteps_guide]{lap_coloursteps_guide()}} bar (order 1).}
}
\value{
A list of ggplot2 layers/scales to add to a plot with \code{+}.
}
\description{
ggplot2's colour-bar and colour-steps guides do not show an \code{NA} entry.
Add \code{+ lap_na_guide()} after a \link{scale_lapidary} scale to draw a single
swatch (an invisible layer on the unused \code{shape} aesthetic) for the
\code{na.value} colour - e.g. hexagons with no wells.
}
\examples{
\dontrun{
library(ggplot2)
data(germany_hex_sample, package = "lapidary")
ggplot(germany_hex_sample) +
  geom_sf(aes(fill = mean_gwl)) +
  scale_fill_lapidary_c("magnitude") +
  lap_na_guide()
}
}
\seealso{
\link{scale_lapidary}
}
