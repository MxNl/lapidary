% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-indicator-scatter.R
\name{lap_plot_indicator_scatter}
\alias{lap_plot_indicator_scatter}
\title{Two indicators against each other}
\usage{
lap_plot_indicator_scatter(
  data,
  x,
  y,
  colour = NULL,
  ...,
  smooth = FALSE,
  rug = FALSE,
  role = "magnitude",
  direction = 1,
  robust = getOption("lapidary.scale_robust", FALSE),
  range = getOption("lapidary.scale_range", FALSE),
  variant = lap_variant(),
  lang = NULL,
  annotate = getOption("lapidary.annotate", "caption"),
  base_size = NULL,
  preset = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{A plain data frame with the \code{x} / \code{y} (/ \code{colour}) columns.}

\item{x, y}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> the columns for the
two axes.}

\item{colour}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> optional third
column to colour points by.}

\item{...}{Passed to the underlying \code{scale_colour_lapidary_c()} (only used
when \code{colour} is given).}

\item{smooth}{Add a linear \code{geom_smooth()} fit.}

\item{rug}{Add marginal rugs.}

\item{role, direction, robust, range}{Passed to the colour scale.}

\item{variant, lang, annotate, base_size, preset, title, subtitle, caption}{See
\link{lap_plot_map}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot}.
}
\description{
A well-level scatter of \code{x} vs \code{y} (columns from \code{\link[=lap_indicators]{lap_indicators()}} /
\code{\link[=lap_summarise_wells]{lap_summarise_wells()}}), optionally coloured by a third indicator on the
lapidary palette. Axis labels carry the catalogue unit.
}
\examples{
\dontrun{
ind <- lap_indicators(gems_ger_sample, c("memory", "flashiness"))
lap_plot_indicator_scatter(ind, ind_memory_weeks, ind_flashiness, smooth = TRUE)
}
}
\seealso{
\link{lap_plot_map}, \code{\link[=lap_plot_distribution]{lap_plot_distribution()}}
}
